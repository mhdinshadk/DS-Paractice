class Node {
  constructor() {
   this.children = {};
   this.isEndWord = false;
  }
}

class Trie {
  constructor () {
    this.root = new Node()
  }

  insert(word) {
    let curr = ds
  }
}